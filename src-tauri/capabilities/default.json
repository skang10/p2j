{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window, plus reading the app version for the header. Custom commands need no extra declaration.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:app:allow-version"
  ]
}
